version: "3"

services:
  php:
    build: ./php
    container_name: ${CONTAINER_NAME}
    ports:
      - ${HOST_PORT}:${CONTAINER_PORT}
    volumes:
      - ./${REPOSITORY_PATH}:/var/www/html
    networks:
      - myenv_php_network
      # - my_infra_network
      # - my_proxy_network

networks:
  myenv_php_network:
    name: myenv_php_network
  # my_infra_network:
  #   external: true
  #   name: my_infra_network
  # my_proxy_network:
  #   external: true
  #   name: my_proxy_network
